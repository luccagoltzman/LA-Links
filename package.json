{
  "name": "la-links",
  "private": true,
  "description": "Hub de links da LA Veículos",
  "scripts": {
    "dev": "sass --watch scss/main.scss:css/main.css --style=expanded --source-map",
    "build": "sass scss/main.scss:css/main.css --style=compressed --no-source-map",
    "preview": "npx --yes serve . -l 5173"
  },
  "engines": {
    "node": ">=20.19.0"
  },
  "dependencies": {
    "sass": "^1.90.0"
  }
}
